name: Build & Deploy Laravel App

on:
  push:
    branches: [test]
    tags: [v*]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    uses: Middle-Earth-Media/build-laravel-app-image/.github/workflows/build-deploy-app-v3.yml@main
    with:
      repository: ${{ github.repository }}
    secrets: inherit
