name: Run Laravel Pint and Build PHP
 
on:
  push:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    # TEST: uses: Middle-Earth-Media/build-laravel-app-image/.github/workflows/build-deploy-app.yml@test
    # PROD: uses: Middle-Earth-Media/build-laravel-app-image/.github/workflows/build-deploy-app.yml@prod
    uses: Middle-Earth-Media/build-laravel-app-image/.github/workflows/build-deploy-app.yml@prod
    with:
      repository: ${{ github.repository }}
      branch: ${{ github.ref_name }}
    secrets: inherit
